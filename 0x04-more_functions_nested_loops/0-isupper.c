#include "main.h"

/**
 * writing uppercase using _isupper.
 * 
 * Return: always 0 if not upercase, 1 if uppercase.
 */

int _isupper(int c)
	char str = 'c'
	if (isupper(str) == 1) {
		_putchar("0");
	}else{
		_putchar("1");
	}

         return 1;
}	 
